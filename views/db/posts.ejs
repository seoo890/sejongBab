<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{title}}</title>
        {% block style %}
        {% endblock %}
        
    </head>
    <body>
        <table border="2">
            <tr>
              <th>번호</th>
              <th>제목</th>
              <th>내용</th>
            </tr>
            {% for items in posts %}
            <tr>
              <td>{{items.id}}</td>
              <td>{{items.title}}</td>
              <td>{{items.content}}</td>
            </tr>
            
            {% endfor %}
            
      
          </table>
        {% block content %}
        {% endblock %}
        {% block script %}
        {% endblock %}
    </body>
</html>